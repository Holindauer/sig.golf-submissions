# Agent instructions

- Work on `beta`. Never commit on `main` without the user’s explicit approval.
- The beta rules and organizer-owned Lean statements live in `leanEthereum/sig.golf-dev`, branch `beta`.
- Keep this repository minimal. Do not reuse the old competition’s contract, track layout, registry, or workflow without reviewing it against the beta rules.
- Never hard-wrap Markdown paragraphs.
